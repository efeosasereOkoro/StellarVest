// Drizzle schema — database tables live here.
//
// Tables are added as we build each story (e.g. investors, contributions,
// cohorts, pools, allocations, and the append-only audit log).
// Empty for now; the connection plumbing is wired in ./index.ts.

export {};
